# This project uses two separate environment files, one per app:
#   frontend/.env  (see frontend/.env.example)
#   backend/.env   (see backend/.env.example)
#
# Copy each of those example files to .env in their respective folder
# and fill in real values. Do not commit .env files.
